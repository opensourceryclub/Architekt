# Directory Structure

- `commands/` - code for every supported command. These must be linked to a
console command via `commander` in `bin/architekt.js`.
- `config.js` - global configuration object
- `helpers` - builtin helper methods, available by default in any Architekt
project.
